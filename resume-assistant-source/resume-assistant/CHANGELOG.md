# Changelog

All notable changes to the Resume Assistant skill will be documented in this file.

## [v0.2.0] - 2026-02-01

### 🎉 Major Features

#### PDF Generation with Perfect Chinese Support
- **完全重写PDF生成引擎**：从reportlab切换到fpdf2库，实现了完美的中文字符渲染
- **自动字体嵌入**：自动下载并嵌入Noto Sans SC字体，确保所有中文字符正确显示
- **专业排版**：保持与HTML版本一致的视觉效果，包括颜色、字体大小和布局

#### DOCX格式支持
- **新增Word文档生成**：添加完整的DOCX格式简历生成功能
- **格式保留**：支持粗体、颜色、字体大小、项目符号等专业格式
- **中文兼容**：完美支持中文字符，无需额外配置

#### 智能用户状态检测
- **应届生/在职人员识别**：根据`is_fresh_graduate`标志自动调整简历结构
- **动态章节排序**：
  - 应届生：教育背景 → 工作经历 → 项目经验
  - 在职人员：工作经历 → 项目经验 → 教育背景
- **全格式支持**：HTML、PDF、DOCX三种格式均支持动态排序

#### HTML打印优化
- **所见即所得打印**：使用`print-color-adjust: exact`保持屏幕显示与打印输出一致
- **A4规格优化**：精确控制页边距和分页，适合直接打印
- **字体大小保留**：打印时保持屏幕字体大小，避免缩放

### 🔧 Technical Improvements

#### PDF生成脚本 (`scripts/create_pdf_resume.py`)
```python
# 新实现亮点
- 使用fpdf2库替代reportlab
- 自动字体管理：add_font('NotoSans', '', '/tmp/fonts/NotoSansSC.ttf')
- 响应式布局：标题左对齐，日期右对齐
- 多行文本支持：使用multi_cell处理长段落
- 项目符号优化：精确控制缩进和间距
```

#### DOCX生成脚本 (`scripts/create_docx_resume.py`)
```python
# 核心功能
- 基于python-docx库
- 专业格式化：字体、颜色、段落间距
- 边框和下划线：使用paragraph_format.border_bottom
- 灵活布局：使用制表符实现左右对齐
- 中文支持：UTF-8编码，无需额外配置
```

#### Web简历模板优化 (`assets/templates/web-resume-modern.html`)
```css
/* 关键改进 */
@media print {
    * {
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
        color-adjust: exact !important;
    }

    @page {
        size: A4;
        margin: 12mm 15mm;
    }

    /* 保留屏幕字体大小 */
    .name { font-size: 32px; }
    .section-title { font-size: 20px; }
}
```

### 📝 Data Structure Updates

#### 新增字段：`is_fresh_graduate`
```json
{
  "is_fresh_graduate": true,  // 应届生标志
  "name": "张三",
  "title": "理工科本科 | 产品工程师求职",
  // ... 其他字段
}
```

#### 兼容性
- **向后兼容**：未设置`is_fresh_graduate`时默认为`false`（在职人员模式）
- **全格式支持**：HTML、PDF、DOCX均识别此字段

### 🐛 Bug Fixes

1. **修复PDF中文乱码问题**
   - 问题：使用reportlab时中文显示为方块或空白
   - 原因：reportlab不支持OTF字体格式，且字体注册机制复杂
   - 解决：切换到fpdf2库，直接嵌入TTF字体

2. **修复HTML打印颜色丢失**
   - 问题：打印时蓝色标题变成黑色
   - 解决：添加`print-color-adjust: exact`强制保留颜色

3. **修复DOCX日期对齐问题**
   - 问题：日期无法靠右对齐
   - 解决：使用制表符`\t`实现左右对齐

### 📦 Dependencies

#### 新增依赖
- `fpdf2>=2.8.1` - PDF生成（替代reportlab）
- `python-docx>=1.1.0` - DOCX生成

#### 移除依赖
- `reportlab` - 已弃用，由fpdf2替代

### 📚 Documentation

#### 新增文档
- `CHANGELOG.md` - 版本更新日志（本文件）
- `README.md` - 更新使用说明，添加三种格式生成示例

#### 更新文档
- `SKILL.md` - 添加PDF/DOCX生成说明
- 代码注释 - 所有脚本添加详细的中文注释

### 🎯 Usage Examples

#### 生成HTML简历
```bash
python scripts/create_web_resume.py \
    --data outputs/resume_data.json \
    --template web-resume-modern \
    --output outputs/resume.html
```

#### 生成PDF简历
```bash
python scripts/create_pdf_resume.py \
    --data outputs/resume_data.json \
    --output outputs/resume.pdf
```

#### 生成DOCX简历
```bash
python scripts/create_docx_resume.py outputs/resume.docx \
    --data outputs/resume_data.json
```

### ⚡ Performance

- **PDF生成速度**：0.2秒（典型简历）
- **DOCX生成速度**：0.1秒
- **HTML生成速度**：<0.1秒
- **字体下载**：首次使用时自动下载，约2秒

### 🔄 Migration Guide

#### 从v0.1.x升级到v0.2.0

1. **更新依赖**：
```bash
pip uninstall reportlab
pip install fpdf2>=2.8.1 python-docx>=1.1.0
```

2. **数据文件更新（可选）**：
```json
// 添加用户状态字段
{
  "is_fresh_graduate": true,  // 应届生设为true，在职设为false
  // ... 其他字段保持不变
}
```

3. **脚本调用更新**：
```bash
# 旧版PDF生成（已弃用）
python scripts/create_pdf_resume_reportlab.py ...

# 新版PDF生成
python scripts/create_pdf_resume.py --data resume_data.json --output resume.pdf
```

### 🙏 Acknowledgments

- **fpdf2**：提供优秀的PDF生成库和中文支持
- **python-docx**：强大的DOCX文档处理能力
- **Noto Sans SC**：Google开源的优秀中文字体

### 🔮 Future Plans

- [ ] 支持更多简历模板（简约、技术、设计等）
- [ ] 添加简历评分功能
- [ ] 支持在线预览和编辑
- [ ] 集成ATS（申请人跟踪系统）关键词优化
- [ ] 支持多语言（英文、日文等）

---

## [v0.1.0] - 2026-01-15

### Initial Release

- ✅ 五大代理系统：故事挖掘、职位推荐、简历优化、模拟面试、能力提升
- ✅ HTML简历生成
- ✅ 基础模板引擎
- ✅ 中文支持
