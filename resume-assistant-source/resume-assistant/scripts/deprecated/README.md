# 已废弃脚本 (Deprecated Scripts)

⚠️ **此目录中的脚本已废弃，不再维护。**

---

## 废弃脚本列表

### 1. `create_pdf_resume_old.py`
- **功能**: 使用 weasyprint 从 HTML 生成 PDF
- **废弃时间**: v2.0.0 (2026-02-01)
- **废弃原因**:
  - weasyprint 依赖复杂（需要 Cairo, Pango 等系统库）
  - 跨平台安装困难
  - 维护成本高
- **替代方案**: 使用 `scripts/current/create_pdf_resume.py`（基于 fpdf2）

### 2. `create_pdf_resume_reportlab.py`
- **功能**: 使用 reportlab 从 JSON 数据生成 PDF
- **废弃时间**: v2.0.0 (2026-02-01)
- **废弃原因**:
  - reportlab API 复杂，学习曲线陡峭
  - 代码冗长，维护成本高
  - 社区支持不如 fpdf2 活跃
- **替代方案**: 使用 `scripts/current/create_pdf_resume.py`（基于 fpdf2）

---

## 为什么保留这些文件？

虽然已废弃，但我们保留这些脚本用于：

1. **向后兼容**: 旧用户可能依赖这些脚本
2. **参考实现**: 提供不同技术方案的参考
3. **迁移指南**: 帮助用户了解从旧版迁移到新版

---

## 迁移指南

### 从 `create_pdf_resume_old.py` 迁移

**旧版使用方式:**
```bash
python create_pdf_resume_old.py input.html output.pdf
```

**新版使用方式:**
```bash
cd ../current
python create_pdf_resume.py --data resume_data.json --output resume.pdf
```

**主要变化:**
- ❌ 不再需要先生成 HTML
- ✅ 直接从 JSON 生成 PDF
- ✅ 更简单的依赖（只需 fpdf2）

### 从 `create_pdf_resume_reportlab.py` 迁移

**旧版使用方式:**
```bash
python create_pdf_resume_reportlab.py --data resume_data.json --output resume.pdf
```

**新版使用方式:**
```bash
cd ../current
python create_pdf_resume.py --data resume_data.json --output resume.pdf
```

**主要变化:**
- ✅ 相同的命令行参数
- ✅ 相同的 JSON 数据格式
- ✅ 更好的中文支持
- ✅ 更简洁的代码

---

## 安装旧版依赖（不推荐）

如果您确实需要运行这些废弃脚本：

### weasyprint 依赖

```bash
# macOS
brew install cairo pango gdk-pixbuf libffi
pip install weasyprint

# Ubuntu/Debian
sudo apt-get install libcairo2-dev libpango1.0-dev libgdk-pixbuf2.0-dev
pip install weasyprint

# Windows
# 需要手动安装 GTK+ 运行时
pip install weasyprint
```

### reportlab 依赖

```bash
pip install reportlab
```

---

## 技术对比

| 特性 | weasyprint | reportlab | fpdf2 (新版) |
|-----|-----------|-----------|------------|
| 安装难度 | 🔴 困难 | 🟡 中等 | 🟢 简单 |
| 系统依赖 | 需要 | 不需要 | 不需要 |
| API 复杂度 | 🟢 简单 | 🔴 复杂 | 🟢 简单 |
| 中文支持 | 🟡 一般 | 🟡 一般 | 🟢 优秀 |
| 社区活跃度 | 🟡 中等 | 🟡 中等 | 🟢 活跃 |
| 维护成本 | 🔴 高 | 🔴 高 | 🟢 低 |

---

## 常见问题

### Q: 我能继续使用这些废弃脚本吗？

A: 可以，但不推荐。这些脚本不再接受更新和bug修复。强烈建议迁移到新版脚本。

### Q: 旧脚本什么时候会被删除？

A: 暂无删除计划。至少在未来2个主版本（v3.0.0, v4.0.0）中会保留。

### Q: 新旧版本生成的 PDF 有什么区别？

A: 样式可能略有不同，但内容完全一致。新版本的中文渲染更好。

### Q: 如何报告旧脚本的问题？

A: 我们不再维护这些脚本。如遇问题，请考虑迁移到新版本。

---

## 相关链接

- [新版脚本目录](../current/)
- [安装指南](../../../../INSTALLATION.md)
- [故障排查](../../../../TROUBLESHOOTING.md)
- [GitHub Issues](https://github.com/Y1fe1-Yang/resume-assistant-skill/issues)

---

**最后更新**: 2026-02-01
**维护状态**: ❌ 不再维护
